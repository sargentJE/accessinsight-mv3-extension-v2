{
  "name": "accessinsight-mv3-extension",
  "version": "0.2.0",
  "description": "Accessibility inspection browser extension with WCAG 2.2 coverage",
  "private": true,
  "scripts": {
    "test": "node tests/quick-test.js",
    "test:watch": "nodemon tests/quick-test.js",
    "test:unit": "mocha tests/unit/**/*.test.js --require tests/helpers/setup.js",
    "test:integration": "mocha tests/integration/**/*.test.js --require tests/helpers/setup.js",
    "test:coverage": "nyc npm test",
    "lint": "echo 'Linting not configured yet'"
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "jsdom": "^23.2.0",
    "mocha": "^10.2.0",
    "nyc": "^15.1.0",
    "sinon": "^17.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
